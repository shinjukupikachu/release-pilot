apiVersion: v1
kind: Service
metadata:
  name: release-agent
  namespace: release-agent
  labels:
    app: release-agent
spec:
  type: ClusterIP

  selector:
    app: release-agent

  ports:
    - name: http
      port: 80
      targetPort: 8080